..  include:: /Includes.rst.txt


..  _extensionSettings:

==================
Extension Settings
==================

Some general settings for `events2_reserve_connector` can be configured.
Currently, partial template can be extended in TypoScript.
